# Phase 31.10 — Reconnect Offer: Auswahl + `ignored`

> **Agent-Attach:** Diese Datei (nicht Archiv/ganzen Plan).
> Regeln: `@AGENTS.md` · Index: `@docs/IMPLEMENTATION_PLAN.md` · Kontext: 31.9

**Status:** ✅ Erledigt  
**Abhängigkeiten:** 31.9 (Reconnect Soft-Confirm)  
**Ziel:** Im Soft-Confirm (Reconnect + Historie-Bulk) Vorgänge per Checkbox wählen. Default alle markiert. Abgewählte → `upload_state=ignored` (kein Badge, kein Offer). Markierte → Scan → Bulk. Manuell unter Vorgänge weiter nachholbar. Kein „Ignorieren aufheben“.

##### Entscheidungen

| # | Thema | Entscheidung |
|---|--------|----------------|
| 1 | State | Neu: `ignored` (Rust normalize + TS) |
| 2 | Outstanding / Badge | Wie `cancelled`: **nicht** outstanding |
| 3 | Historie Retry | Wie `cancelled`: `canRetry` / Chip / Nachholen |
| 4 | Folder-Missing | Wie `cancelled`: nicht primary folder problem |
| 5 | Cleanup skip | `ignored` gilt als retryable (Ordner behalten) |
| 6 | UI | Checkbox-Liste aller Offer-Einträge; Default checked |
| 7 | Später | Nichts ändern |
| 8 | Jetzt | Abgewählte → `ignored`; Markierte → Scan → Bulk |
| 9 | 0 markiert | Nur Ignorieren, kein Scan |
| 10 | Undo | Kein spezielles Undo — nur „Upload nachholen“ |
| 11 | Copy | „Nicht markierte werden nicht mehr vorgeschlagen. Upload weiter unter Vorgänge möglich.“ |

##### Scope

- [x] Spec + Index / AGENTS / README
- [x] Rust `normalize_upload_state` + Tests; `is_retryable_upload`
- [x] TS `uploadState` + Tests; Historie-Chip Labels
- [x] Dialog: Checkboxen + Hint; Choice trägt `selectedIds`
- [x] App + HistoryDialog: Ignorieren persistieren, dann Bulk
- [x] i18n de / en / es-MX
- [x] `cargo test` + `npm run check`

##### Agent-Prompt

```
Implementiere Phase 31.10 aus @docs/phases/open/31.10-reconnect-upload-select.md
Regeln: @AGENTS.md
Nur 31.10.
```
